{
  "name": "@frictionsets/app",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "GitHub App that files frictionsets as issues and keeps the two in sync.",
  "scripts": {
    "check:types": "tsc",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "frictionsets": "workspace:*",
    "octokit": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  },
  "engines": {
    "node": ">=22"
  }
}
